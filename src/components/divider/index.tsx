import React from "react";

const Divider = () => {
    return (
        <div
            style={{
                height: "2px",
                backgroundColor: "var(--divider)",
                margin: "12px 6px 0 6px",
            }}
        ></div>
    );
};

export default Divider;
